<?php
$count = 0;
$summaryTitle = '';
foreach ($nGrams[3] as $key => $nGram) {
    $summaryTitle .= ($count == 2)
                   ? ('and ' . $key)
                   : ($key . ', ');
    $count++;
    if ($count >= 3) {
        break;
    }
}
$this->layout()->setVariables([
    'htmlHeadTitle' => 'So, to summarize, ' . $summaryTitle,
]);
?>
<div class="main-container columns-2">
    <div class="column column-1">
        <div class="panel">
            <h1><?= $this->escape($summaryTitle); ?></h1>
        </div>
        <div class="panel">
            <h2>Titles</h1>

            <ol>
                <?php foreach ($titleVariations as $titleVariation): ?>
                    <li><?= $this->escape($titleVariation); ?></li>
                <?php endforeach ?>
            </ol>
        </div>
    </div>
    <div class="column column-2">
        <div class="panel follow">
            <h2>Details</h2>

            <h3>URL</h3>
            <?= $this->escape($summaryEntity->getWebpage()->getUrl()); ?>
        </div>
    </div>
</div>
